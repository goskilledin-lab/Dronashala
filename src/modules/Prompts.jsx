import { Eyebrow } from "../ui/Eyebrow.jsx";
import { Empty } from "../ui/Empty.jsx";

export function Prompts() {
  return (
    <div>
      <Eyebrow>Prompt Library</Eyebrow>
      <Empty text="Prompts module — full build lands in Step 7." />
    </div>
  );
}
