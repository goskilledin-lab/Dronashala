import { Eyebrow } from "../ui/Eyebrow.jsx";
import { Empty } from "../ui/Empty.jsx";

export function Notes() {
  return (
    <div>
      <Eyebrow>Notes</Eyebrow>
      <Empty text="Notes module — full build lands in Step 7." />
    </div>
  );
}
